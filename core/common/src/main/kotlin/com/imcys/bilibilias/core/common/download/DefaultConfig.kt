package com.imcys.bilibilias.core.common.download

object DefaultConfig {
    const val defaultNameRule = "{BV}/{FILE_TYPE}/{P_TITLE}_{CID}.{FILE_TYPE}"
    const val defaultStorePath = "bilibilias/download"
}
